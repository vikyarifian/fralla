package components

import (
    "fmt"
)

templ SuccessAlert(message string, form string) {
    <div id={fmt.Sprintf("success_%s",form)} hx-swap-oob="true">
        <label class="text-success">{ message }</label>
    </div>        
}